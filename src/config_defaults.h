#ifndef MYWM_CONFIG_DEFAULTS_H
#define MYWM_CONFIG_DEFAULTS_H

#include "config.h"

/* Заполняет cfg встроенными значениями по умолчанию (соответствуют
   примеру config.conf из задания). */
void config_defaults(wm_config_t *cfg);

#endif
